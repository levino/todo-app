{
  "name": "@family-todo/e2e",
  "private": true,
  "description": "Full-stack docker-compose e2e harness (oauth2-proxy + dex + frontend + mcp)",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.57.0"
  }
}
