# Docker Compose files for this project:
#
# Development: docker compose -f docker-compose.dev.yaml up
# Testing:     docker compose -f docker-compose.test.yaml up -d pocketbase
# Production:  docker compose -f docker-compose.coolify.yaml up

# Default: alias for dev
services:
  pocketbase:
    extends:
      file: docker-compose.dev.yaml
      service: pocketbase

  frontend:
    extends:
      file: docker-compose.dev.yaml
      service: frontend

  mcp:
    extends:
      file: docker-compose.dev.yaml
      service: mcp

volumes:
  frontend_node_modules:
  mcp_node_modules:
  mcp_data:
