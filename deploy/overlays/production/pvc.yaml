apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-app-data
  namespace: todo-app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
